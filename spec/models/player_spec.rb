require 'spec_helper'

RSpec.describe Player do

  context "on joining a game" do
    context "with valid criteria" do

    end
    context "with invalid criteria" do
      it "should not occur more than once in the same game" do

      end
    end
  end


end
